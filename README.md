# Link-collector
Developed with django ,  scrapes and delete link.

create virtual env.
pip install django
pip install requests and beautifullsoup  python library
django-admin startproject and startapp..giving app a name

>>followed basic MVU logic 
register models admin.site.register(Link)
